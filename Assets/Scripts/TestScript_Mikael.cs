using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestScript_Mikael : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        print("Hello Git World");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
